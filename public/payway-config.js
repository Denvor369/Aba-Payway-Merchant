// payway-config.js — Real site config (edit if needed)
window.PAYWAY_API_BASE = "http://localhost:3000";   // backend base
window.PAYWAY_BAG_KEY  = "bag";                     // your cart key in localStorage
window.PAYWAY_BUTTON_ID = "paywayBtn";              // DEDICATED PayWay button id (to avoid conflicts)
